# Web server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Download .pt files from Hugging Face when not present locally
huggingface_hub>=0.21.0

# Tokenizer fallback when not bundled inside the .pt checkpoint
transformers>=4.38.0
